<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="uk.co.appsbystudio.geoshare">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <application
        android:name=".Application"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true">
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/api_key" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name=".login.LoginActivity"
            android:label="@string/app_name"
            android:theme="@style/LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".utils.setup.InitialSetupActivity"
            android:label="Initial Setup"
            android:theme="@style/LoginActivity" />
        <activity
            android:name=".MainActivity"
            android:launchMode="singleTop"
            android:theme="@style/MainTheme" />
        <activity
            android:name=".friends.pages.FriendSearchActivity"
            android:theme="@style/FriendsTheme" />
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Theme.AppCompat" />
        <activity
            android:name=".friends.FriendsManager"
            android:label="@string/title_activity_friends_manager"
            android:parentActivityName=".MainActivity"
            android:theme="@style/FriendsTheme">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="uk.co.appsbystudio.geoshare.MainActivity" />
        </activity>
        <activity
            android:name=".friends.pages.FriendInfoActivity"
            android:label="@string/title_activity_friend_info"
            android:parentActivityName=".friends.FriendsManager"
            android:theme="@style/FriendsInfoTheme">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="uk.co.appsbystudio.geoshare.friends.FriendsManager" />
        </activity>

        <service
            android:name=".services.TrackingService"
            android:exported="false" />
    </application>

</manifest>